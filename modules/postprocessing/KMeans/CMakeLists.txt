INCLUDE_DIRECTORIES(${BIOSANDBOX_SOURCE}/include ${OpenCV_INCLUDE_DIRS})


ADD_LIBRARY( KMeans SHARED kmeans )
TARGET_LINK_LIBRARIES( KMeans ${OpenCV_LIBS} )
SET_PROPERTY(TARGET KMeans PROPERTY FOLDER "postprocessing")


INSTALL(TARGETS KMeans DESTINATION bin/modules )

