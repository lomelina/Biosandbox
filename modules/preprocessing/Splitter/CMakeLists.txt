INCLUDE_DIRECTORIES(${BIOSANDBOX_SOURCE}/include ${OpenCV_INCLUDE_DIRS})

ADD_LIBRARY( Splitter SHARED main )
TARGET_LINK_LIBRARIES( Splitter ${OpenCV_LIBS} )
SET_PROPERTY(TARGET Splitter PROPERTY FOLDER "preprocessing")

INSTALL(TARGETS Splitter DESTINATION bin/modules )
